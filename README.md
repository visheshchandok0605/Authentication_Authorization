# 🔐 JWT Generation & Validation (Python)

## 📝 Problem Statement

Design and implement a program that performs **JSON Web Token (JWT) generation and validation** for user authentication.

The program should:

- Generate a JWT containing the following claims:
  ```json
  {
    "sub": "<user-id>",
    "role": "user"
  }
